"""HTTP REST API for BeeWings — a thin wrapper over the existing core
functions (segmentation, ML landmark detection, indices, export).

Run locally with ``beewings-api`` (or ``uvicorn beewings.api.app:app``).
"""
